uv sync
source .venv/bin/activate

if ! cat .git/hooks/pre-commit 2> /dev/null | grep -q 'File generated by pre-commit: https://pre-commit.com'; then
    pre-commit install
fi

source_env_if_exists .envrc.private
