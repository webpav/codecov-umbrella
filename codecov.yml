ignore:
  - "**tests**/test_*.py"

flag_management:
  default_rules:
    carryforward: true
    statuses:
      - type: project
        target: auto
        threshold: 1
      - type: patch
        target: 90

codecov:
  wait_for_ci: false
  require_ci_to_pass: false
